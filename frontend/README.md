# Frontend

## Run

```bash
npm install
npm run dev
```

The current frontend is a Phase 1 shell:

- React + Vite scaffold
- dashboard-first layout
- seeded Kanban board data
- visual language aligned to the design reference

The next frontend step is to replace the seeded data with API-backed state and add task details, agents, and runs views.

